import EventsClient from '@/src/components/EventsClient';

export const metadata = {
  title: 'Initiatives | IERF',
  description: 'Explore the initiatives, dialogues, and expert talks hosted by the India Eurasia Research Forum.',
};

export default function Events() {
  return <EventsClient />;
}
