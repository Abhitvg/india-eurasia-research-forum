import ContactClient from '@/src/components/ContactClient';

export const metadata = {
  title: 'Contact Us | IERF',
  description: 'Get in touch with the India Eurasia Research Forum for collaboration, inquiries, and media requests.',
};

export default function Contact() {
  return <ContactClient />;
}
